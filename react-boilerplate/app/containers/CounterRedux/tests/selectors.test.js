// import { selectCounterReduxDomain } from '../selectors';

describe('selectCounterReduxDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
