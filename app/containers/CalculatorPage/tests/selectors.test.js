// import { selectCalculatorPageDomain } from '../selectors';

describe('selectCalculatorPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
