/*
 * CalculatorPage Messages
 *
 * This contains all the text for the CalculatorPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CalculatorPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CalculatorPage container!',
  },
});
